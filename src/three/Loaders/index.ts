/**
 * Asset loader utilities.
 *
 * Future usage:
 *  - GLTF / GLB model loading
 *  - Texture loading with progress
 *  - HDR environment map loading
 *  - Audio loading
 *
 * All loaders should use drei's `useLoader` / `useGLTF` / `useTexture`
 * helpers under the hood and expose project-specific wrappers here.
 */
